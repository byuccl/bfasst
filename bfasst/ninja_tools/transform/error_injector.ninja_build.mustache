build {{ log_path }} {{ corrupt_netlist_path }}: inject | {{ build_dir }}/xray/{{ top }}_reversed.v
    build_dir = {{ build_dir }}
    seed = {{ seed }}
    error_type = {{ error_type }}
    log_path = {{ log_path }}

