rule rand_soc
    command = python {{ bfasst_path }}/third_party/rand_soc/rand_soc/main.py $design_dir_path $config_path --seed $seed --part $part
    description = generate a random SoC in $design_dir_path

